<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">LOGO</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if (!isAuthenticated) { %>
      <li class="nav-item">
        <a href='/' class="<%= (path === '/')? 'active nav-link' : 'nav-link' %>">Shop</a>
      </li>
      <li class="nav-item">
        <a href='/products' class="<%= (path === '/products')? 'active nav-link' : 'nav-link' %>">Products</a>
      </li>
      <% } %>
      <% if (isAuthenticated) { %>
      <li class="nav-item">
        <a href='/cart' class="<%= (path === '/cart')? 'active nav-link' : 'nav-link' %>">Cart</a>
      </li>
      <li class="nav-item">
        <a href='/orders' class="<%= (path === '/orders')? 'active nav-link' : 'nav-link' %>">Orders</a>
      </li>

      <li class="nav-item">
        <a href='/admin/products' class="<%= (path === '/admin/products')? 'active nav-link' : 'nav-link' %>">
          Admin Products</a>
      </li>
      <li class="nav-item">
        <a href='/admin/add-product' class="<%= (path === '/admin/add-product')? 'active nav-link' : 'nav-link' %>">Add
          product</a>
      </li>

      <% } %>
    </ul>
    <div class="form-inline my-2 my-lg-0">
      <% if (!isAuthenticated) { %>
      <a href='/login'
        class="<%= (path === '/login')? 'active nav-link my-2 my-sm-0' : 'nav-link my-2 my-sm-0' %>">Login</a>
      <% } else { %>
      <form action="/logout" method="POST">
        <button class="<%= (path === '/login')? 'active nav-link my-2 my-sm-0' : 'nav-link my-2 my-sm-0' %>"
          type="submit">LogOut</button>
      </form>
      <% } %>
    </div>
  </div>
</nav>